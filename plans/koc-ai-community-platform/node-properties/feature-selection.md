# `feature-selection` — Feature selection

**Category:** Transform · **Ports:** Table → Table · **Handler:** `FeatureSelectionHandler`

Drop near-constant numeric features.

## Parameters
| `key` | Label | Type | Default | Required | Options / Range | In UI today |
|---|---|---|---|---|---|---|
| `count` | Min non-default count | Number | `1` | no | ≥ 1 | ✅ yes |

## Panel on click
One number field, pre-filled `1`.
